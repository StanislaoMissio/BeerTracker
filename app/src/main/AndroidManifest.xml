<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.android.beertracker">

    <uses-permission
        android:name="android.permission.INTERNET"
        android:maxSdkVersion="24" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".activity.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".activity.LoginActivity"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".activity.EstiloActivity" />
        <activity
            android:name=".activity.Motorista"
            android:label="@string/title_activity_motorista"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.Estilos"
            android:label="@string/title_activity_estilos"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.New_role"
            android:label="@string/title_activity_new_role"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name=".activity.Amigos"
            android:label="@string/title_activity_amigos"
            android:theme="@style/AppTheme.NoActionBar">
        </activity>
        <activity android:name=".activity.EstiloDetailPageActivity">
        </activity>
    </application>

</manifest>