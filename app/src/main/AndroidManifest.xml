<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.android.beertracker">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_beer_tracker"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_beer_tracker_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme.NoActionBar">

        <activity android:name=".activity.MainActivity"
                  android:label="@string/app_name">
        </activity>

        <activity android:name=".activity.LoginActivity">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <activity android:name=".activity.CadastroActivity">
        </activity>

        <activity android:name=".activity.MotoristaActivity">
        </activity>

        <activity android:name=".activity.ChosenDriverActivity">
        </activity>

        <activity android:name=".activity.StyleDetailPageActivity">
        </activity>

        <activity android:name=".activity.StyleActivity">
        </activity>

    </application>

</manifest>
